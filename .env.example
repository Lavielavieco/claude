# Copy to .env and fill in. .env is gitignored and never committed.
# Generate fresh keys after any exposure — do not reuse keys that have been pasted in chat.

EODHD_API_KEY=
UNUSUAL_WHALES_API_KEY=
POLYGON_API_KEY=
